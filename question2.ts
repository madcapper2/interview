// Look at the gone-with-the-wind.json file and create a function that gets a list of pages and returns the list ordered by page number.
// Write the function below and provide a name of the function

function _________________________(pages:[]): [] {
    //write function here
}
